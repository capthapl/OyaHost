using System;

namespace OyaHost.BLL.Universal
{
    public enum SystemVersion
    {
        Fedora30 = 0,
    }
    public enum CommandResponseStatusCode{
        Success = 1,
        Error = 0
    }
}